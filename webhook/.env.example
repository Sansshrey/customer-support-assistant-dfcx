PORT=8080
LOG_LEVEL=INFO
FLASK_DEBUG=0
# Optional: if set, /webhook requires header X-Webhook-Secret to match this value.
# Configure the same value in the Dialogflow CX webhook's custom header.
WEBHOOK_SHARED_SECRET=
